# Pinned environment used while preparing the companion paper. For a normal
# install use `pip install .` (which reads pyproject.toml); only fall back to
# this lockfile when reproducing the exact environment the paper was run in.
#
# Note: this file does not list every transitive dependency and may be
# incomplete relative to pyproject.toml (e.g. scipy, h5py). Treat it as a
# historical snapshot, not the source of truth.

# Core dependencies
astropy==5.1
numpy==1.21.5
pandas==2.0.3
pixell==0.16.0
tomli==2.2.1

# Optional dependencies
psutil==5.9.2
IPython==8.12.3
ipywidgets==8.1.2
jupyter==1.0.0
jupyter_server==2.14.0
jupyterlab==4.1.8
matplotlib==3.6.1
mpi4py==3.1.6

# Development and testing dependencies
pytest==8.3.2
pytest-cov==5.0.0